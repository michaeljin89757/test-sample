# test-sample
This repo contains a python script, that has a sample input of IP address and requests.
The script will form a report about the IP address has the highest request number and statistics of number of all status code returned.
